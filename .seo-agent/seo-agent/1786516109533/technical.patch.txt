File: TradePulse/src/engine/mock-price-engine.ts
Finding: technical-robots-error
Category: technical
Recommendation: Failed to fetch robots.txt; verify network and URL.
Patch:
-import type {
+ /**
-  MockTicker,
+25:  * Per-ticker volatility scale for the log random walk (σ per one-second step).
-  PricePoint,
+26:
-  PriceSnapshot,
-  PriceUpdateListener,
-  TickerPriceListener,
-  TickerPriceSnapshot,
-} from './types';
-import { MOCK_TICKERS } from './types';
-
-/** Display / storage rounding per asset (reduces float noise in history and snapshots). */
-const TICKER_DECIMALS: Record<MockTicker, number> = {
-  BTC: 2,
-  ETH: 2,
-  SOL: 4,
-};
-
-/** Starting mids before the first random-walk step (override via options). */
-const DEFAULT_INITIAL_PRICES: Record<MockTicker, number> = {
-  BTC: 98_500,
-  ETH: 3_350,
-  SOL: 185,
-};
-
-/**
- * Per-ticker volatility scale for the log random walk (σ per one-second step).
- * Tuned for plausible on-screen movement, not calibrated to real markets.
- */
-const DEFAULT_VOLATILITY: Record<MockTicker, number> = {
-  BTC: 0.00035,
-  ETH: 0.00045,
-  SOL: 0.00055,
-};
-
-export interface MockPriceEngineOptions {
-  /** Tick cadence; default 1000 ms */
-  readonly intervalMs?: number;
-  /** Cap memory for each ticker’s series; oldest points dropped first (default 100) */
-  readonly maxHistoryPerTicker?: number;
-  readonly initialPrices?: Partial<Record<MockTicker, number>>;
-  readonly volatility?: Partial<Record<MockTicker, number>>;
-}
-
-function roundToDecimals(value: number, decimals: number): number {
-  const factor = 10 ** decimals;
-  return Math.round(value * factor) / factor;
-}
-
-/**
- * Box–Muller transform — standard normal sample.
- * Sufficient for mock price diffusion; not cryptographic RNG.
- */
-function gaussianRandom(): number {
-  let u = 0;
-  let v = 0;
-  while (u === 0) {
-    u = Math.random();
-  }
-  while (v === 0) {
-    v = Math.random();
-  }
-  return Math.sqrt(-2.0 * Math.log(u)) * Math.cos(2.0 * Math.PI * v);
-}
-
-/**
- * Pure TypeScript mock market data service: geometric random walk on three tickers,
- * fixed 1 s cadence by default, observer pattern for subscribers, no React.
- */
-export class MockPriceEngine {
-  private readonly intervalMs: number;
-  private readonly maxHistoryPerTicker: number;
-  private readonly tickerVolatility: Record<MockTicker, number>;
-
-  private intervalId: ReturnType<typeof setInterval> | null = null;
-  /** Global tick subscribers — cleared on `destroy()`; remove via returned unsubscribe to avoid retaining callbacks. */
-  private readonly allListeners = new Set<PriceUpdateListener>();
-  /** Per-ticker subscribers — empty ticker keys are deleted so the map does not grow unbounded. */
-  private readonly tickerListeners = new Map<MockTicker, Set<TickerPriceListener>>();
-
-  private readonly currentPrices: Record<MockTicker, number>;
-  private readonly history: Record<MockTicker, PricePoint[]>;
-  private tickSequence = 0;
-
-  constructor(options: MockPriceEngineOptions = {}) {
-    this.intervalMs = options.intervalMs ?? 1000;
-    this.maxHistoryPerTicker = options.maxHistoryPerTicker ?? 100;
-    this.tickerVolatility = { ...DEFAULT_VOLATILITY, ...options.volatility };
-
-    this.currentPrices = { ...DEFAULT_INITIAL_PRICES, ...options.initialPrices };
-    for (const ticker of MOCK_TICKERS) {
-      this.currentPrices[ticker] = roundToDecimals(
-        this.currentPrices[ticker],
-        TICKER_DECIMALS[ticker],
-      );
-    }
-
-    this.history = {
-      BTC: [],
-      ETH: [],
-      SOL: [],
-    };
-
-    const t0 = Date.now();
-    for (const ticker of MOCK_TICKERS) {
-      this.appendHistoryPoint(ticker, this.currentPrices[ticker], t0);
-    }
-    this.tickSequence = 1;
-  }
-
-  /**
-   * Whether the interval timer is currently scheduled.
-   */
-  isRunning(): boolean {
-    return this.intervalId !== null;
-  }
-
-  /**
-   * Subscribe to ticks for one symbol only. Each emission passes a fresh `TickerPriceSnapshot` (copied history slice).
-   * Call the returned function to remove the callback — the engine holds no strong reference to it after that.
-   * Same function reference subscribed twice is a no-op the second time (Set semantics).
-   */
-  subscribe(ticker: MockTicker, listener: TickerPriceListener): () => void {
-    let set = this.tickerListeners.get(ticker);
-    if (set === undefined) {
-      set = new Set();
-      this.tickerListeners.set(ticker, set);
-    }
-    set.add(listener);
-    return () => {
-      this.removeTickerListener(ticker, listener);
-    };
-  }
-
-  /**
-   * Subscribe to every tick with the full multi-ticker snapshot.
-   * Unsubscribe via the returned disposer when the consumer unmounts or no longer needs updates.
-   */
-  subscribeAll(listener: PriceUpdateListener): () => void {
-    this.allListeners.add(listener);
-    return () => {
-      this.allListeners.delete(listener);
-    };
-  }
-
-  /**
-   * Current state without advancing the walk — useful for hydration or SSR-style reads.
-   */
-  getSnapshot(): PriceSnapshot {
-    return this.materializeSnapshot(Date.now());
-  }
-
-  /**
-   * Begins emitting one snapshot per `intervalMs`. No-op if already running.
-   */
-  start(): void {
-    if (this.intervalId !== null) {
-      return;
-    }
-    this.intervalId = setInterval(() => {
-      this.runTick();
-    }, this.intervalMs);
-  }
-
-  /**
-   * Stops the timer; latest prices and history are retained. No-op if already stopped.
-   */
-  stop(): void {
-    if (this.intervalId === null) {
-      return;
-    }
-    clearInterval(this.intervalId);
-    this.intervalId = null;
-  }
-
-  /**
-   * Tears down the timer and clears subscribers. Optional disposal before unload.
-   */
-  destroy(): void {
-    this.stop();
-    this.allListeners.clear();
-    this.tickerListeners.clear();
-  }
-
-  private runTick(): void {
-    const emittedAtMs = Date.now();
-    for (const ticker of MOCK_TICKERS) {
-      const next = this.stepLogRandomWalk(ticker);
-      this.currentPrices[ticker] = next;
-      this.appendHistoryPoint(ticker, next, emittedAtMs);
-    }
-    this.tickSequence += 1;
-
-    if (this.allListeners.size > 0) {
-      this.notifyAllListeners(this.materializeSnapshot(emittedAtMs));
-    }
-    for (const ticker of MOCK_TICKERS) {
-      const set = this.tickerListeners.get(ticker);
-      if (set !== undefined && set.size > 0) {
-        const update = this.materializeTickerSnapshot(ticker, emittedAtMs);
-        this.notifyTickerListeners(set, update);
-      }
-    }
-  }
-
-  /**
-   * Geometric Brownian-style step: S' = S * exp(σ * Z), Z ~ N(0,1).
-   * Floored to a positive minimum derived from decimal precision.
-   */
-  private stepLogRandomWalk(ticker: MockTicker): number {
-    const prev = this.currentPrices[ticker];
-    const sigma = this.tickerVolatility[ticker];
-    const z = gaussianRandom();
-    const raw = prev * Math.exp(z * sigma);
-    const decimals = TICKER_DECIMALS[ticker];
-    const minPositive = 10 ** -decimals;
-    const bounded = Math.max(minPositive, raw);
-    return roundToDecimals(bounded, decimals);
-  }
-
-  private appendHistoryPoint(
-    ticker: MockTicker,
-    price: number,
-    timestamp: number,
-  ): void {
-    const series = this.history[ticker];
-    series.push({ ticker, price, timestamp });
-    if (series.length > this.maxHistoryPerTicker) {
-      series.splice(0, series.length - this.maxHistoryPerTicker);
-    }
-  }
-
-  /**
-   * Deep-copies visible state so external code cannot mutate internal buffers.
-   */
-  private materializeSnapshot(emittedAtMs: number): PriceSnapshot {
-    const prices = { ...this.currentPrices } as Record<MockTicker, number>;
-    const history = {} as Record<MockTicker, readonly PricePoint[]>;
-    for (const ticker of MOCK_TICKERS) {
-      history[ticker] = [...this.history[ticker]];
-    }
-    return {
-      prices,
-      history,
-      tickSequence: this.tickSequence,
-      emittedAtMs,
-    };
-  }
-
-  private materializeTickerSnapshot(
-    ticker: MockTicker,
-    emittedAtMs: number,
-  ): TickerPriceSnapshot {
-    return {
-      ticker,
-      price: this.currentPrices[ticker],
-      history: [...this.history[ticker]],
-      tickSequence: this.tickSequence,
-      emittedAtMs,
-    };
-  }
-
-  private removeTickerListener(
-    ticker: MockTicker,
-    listener: TickerPriceListener,
-  ): void {
-    const set = this.tickerListeners.get(ticker);
-    if (set === undefined) {
-      return;
-    }
-    set.delete(listener);
-    if (set.size === 0) {
-      this.tickerListeners.delete(ticker);
-    }
-  }
-
-  private notifyAllListeners(snapshot: PriceSnapshot): void {
-    const copy = [...this.allListeners];
-    for (const listener of copy) {
-      try {
-        listener(snapshot);
-      } catch (err) {
-        console.error('[MockPriceEngine] subscribeAll listener error', err);
-      }
-    }
-  }
-
-  private notifyTickerListeners(
-    set: Set<TickerPriceListener>,
-    update: TickerPriceSnapshot,
-  ): void {
-    const copy = [...set];
-    for (const listener of copy) {
-      try {
-        listener(update);
-      } catch (err) {
-        console.error(
-          '[MockPriceEngine] subscribe listener error',
-          update.ticker,
-          err,
-        );
-      }
-    }
-  }
-}
-